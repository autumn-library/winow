#Использовать ".."

Шаблон = Новый Шаблон();

Шаблон.ТекстШаблона = 
"multyline
|text {{ Модель.Имя }} template
|{% Для каждого ТекСимвол из Модель.Массив Цикл %}
|simbol is {{ ТекСимвол }}
|{% КонецЦикла; %}
|thats all";

Массив = Новый Массив;
Массив.Добавить(1);
Массив.Добавить(2);
Массив.Добавить("qwert");
Массив.Добавить(ТекущаяДата());


Шаблон.Модель = Новый Структура("Имя, Массив", "nikita", Массив);

ИзШаблона = Шаблон.СформироватьТекст();

Сообщить(ИзШаблона);