#Использовать autumn
#Использовать ".."
#Использовать asserts

// Тесты выполняются для обеих реализаций веб-сервера winow.
// Обе поднимаются один раз на разных портах и живут до конца прогона,
// поэтому переключение реализации между тестами не требует перезапуска.
Перем КонтекстПрикладной;
Перем КонтекстНативный;

// Поделка и порт реализации, выбранной для текущего теста.
Перем Поделка;
Перем Порт;

// Сколько миллисекунд ждать готовности сервера и с каким шагом опрашивать.
Перем ПределОжиданияСервера;
Перем ШагОжиданияСервера;

&ТестовыйНабор(Характер = "Одиночка")
Процедура ПриСозданииОбъекта() Экспорт
	ПределОжиданияСервера = 15000;
	ШагОжиданияСервера = 50;
КонецПроцедуры

&ПослеВсех
Процедура ОстановитьСерверы() Экспорт
	ОстановитьСервер(КонтекстПрикладной);
	ОстановитьСервер(КонтекстНативный);
КонецПроцедуры

// Выбирает реализацию веб-сервера для текущего теста,
// поднимая её при первом обращении.
//
// Параметры:
//  Прикладной - Булево - Истина для прикладного сервера, Ложь для нативного.
//
Процедура ВключитьСервер(Прикладной)

	Если Прикладной Тогда
		Если КонтекстПрикладной = Неопределено Тогда
			КонтекстПрикладной = ПоднятьСервер(Истина, 3333);
		КонецЕсли;
		Контекст = КонтекстПрикладной;
	Иначе
		Если КонтекстНативный = Неопределено Тогда
			КонтекстНативный = ПоднятьСервер(Ложь, 3334);
		КонецЕсли;
		Контекст = КонтекстНативный;
	КонецЕсли;

	Поделка = Контекст.Поделка;
	Порт = Контекст.Порт;

КонецПроцедуры

Функция ПоднятьСервер(Прикладной, НомерПорта)

	ЗначенияДеталек = Новый Соответствие();
	ЗначенияДеталек.Вставить("winow.ИспользоватьПрикладнойСервер", Прикладной);
	ЗначенияДеталек.Вставить("winow.Порт", НомерПорта);

	Совет = Новый СоветДругогоМастера();
	Совет.ЗначенияДеталек(ЗначенияДеталек);

	НоваяПоделка = Новый Поделка(Совет);
	НоваяПоделка.ЗапуститьПриложение();

	ВебСервер = НоваяПоделка.НайтиЖелудь("ВебСервер");
	ФоновыеЗадания.Выполнить(ВебСервер, "Старт");
	ДождатьсяГотовностиСервера(НомерПорта);

	Настройки = НоваяПоделка.НайтиЖелудь("Настройки");
	Настройки.ЗадержкаПередЧтениемСокета = 400;
	Настройки.ЗадержкаПередЗакрытиемСокета = 300;
	Настройки.ИнтервалПроверкиСоединенийСерверныхСобытий = 100;

	Возврат Новый Структура("Поделка, Порт", НоваяПоделка, НомерПорта);

КонецФункции

// Ждёт, пока сервер начнёт отвечать на запросы.
//
// Сервер стартует фоновым заданием, а маршруты регистрируются рогатками, тоже
// параллельно. Фиксированная пауза тут ненадёжна: на нагруженной машине
// регистрация в неё не укладывается, и первые запросы получают 404 на
// существующем адресе. Поэтому ждём не время, а готовность - и на свободной
// машине выходим сразу, не тратя секунду впустую.
Процедура ДождатьсяГотовностиСервера(НомерПорта)

	КрайнийСрок = ТекущаяУниверсальнаяДатаВМиллисекундах() + ПределОжиданияСервера;

	Пока ТекущаяУниверсальнаяДатаВМиллисекундах() < КрайнийСрок Цикл

		Если СерверОтвечает(НомерПорта) Тогда
			Возврат;
		КонецЕсли;

		Приостановить(ШагОжиданияСервера);

	КонецЦикла;

	ВызватьИсключение СтрШаблон("Сервер на порту %1 не ответил за %2 мс",
		НомерПорта, ПределОжиданияСервера);

КонецПроцедуры

Функция СерверОтвечает(НомерПорта)

	Попытка
		Соединение = Новый HTTPСоединение("http://localhost", НомерПорта, , , , 5);
		Возврат Соединение.Получить(Новый HTTPЗапрос("/tests/checkup")).КодСостояния = 200;
	Исключение
		// Порт ещё не слушают - соединение не устанавливается.
		Возврат Ложь;
	КонецПопытки;

КонецФункции

Процедура ОстановитьСервер(Контекст)

	Если Контекст = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Попытка
		Контекст.Поделка.НайтиЖелудь("ВебСервер").Стоп();
	Исключение
		// Сервер уже остановлен - ронять из-за этого прогон незачем.
	КонецПопытки;

КонецПроцедуры

Функция ГетЗапрос(АдресТеста, Заголовки = Неопределено)
	Соединение = Новый HTTPСоединение("http://localhost",Порт,,,,5);
	Запрос = Новый HTTPЗапрос(СтрШаблон("/tests/%1", АдресТеста));
	Если Не Заголовки = Неопределено Тогда
		Запрос.Заголовки = Заголовки;
	КонецЕсли;
	Ответ = Соединение.Получить(Запрос);
	Возврат Ответ;
КонецФункции

Функция ПроизвольныйГетЗапрос(АдресТеста, Заголовки = Неопределено)
	Соединение = Новый HTTPСоединение("http://localhost",Порт,,,,5);
	Запрос = Новый HTTPЗапрос(АдресТеста);
	Если Не Заголовки = Неопределено Тогда
		Запрос.Заголовки = Заголовки;
	КонецЕсли;
	Ответ = Соединение.Получить(Запрос);
	Возврат Ответ;
КонецФункции

Функция ПостЗапросТелоИзСтроки(АдресТеста, ТелоЗапроса, Заголовки = Неопределено)
	Соединение = Новый HTTPСоединение("http://localhost",Порт,,,,5);
	Запрос = Новый HTTPЗапрос(СтрШаблон("/tests/%1", АдресТеста));
	Если Не Заголовки = Неопределено Тогда
		Запрос.Заголовки = Заголовки;
	КонецЕсли;
	Запрос.УстановитьТелоИзСтроки(ТелоЗапроса, КодировкаТекста.UTF8);
	Ответ = Соединение.ОтправитьДляОбработки(Запрос);
	Возврат Ответ;
КонецФункции

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьЗапускСервере(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "checkup";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуГетПараметров(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "getparams?animal1=Кошка&animal2=Собака";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Кошка И Собака");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуЗакодированныхРазделителейВГетПараметрах(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "getparams?animal1=Кошка&animal2=Собака%26Хомяк";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Кошка И Собака&Хомяк");

КонецПроцедуры

// Реализации сервера обязаны разбирать адрес одинаково: прикладной разбирает сырой
// HTTP-текст сам, нативному путь отдаёт уже разобранным Kestrel, и разойтись они
// могут незаметно - достаточно поправить декодирование только у одной из них.
// Поэтому сверяется вся батарея адресов целиком, а не один известный случай.
&Тест
Процедура Должен_ПроверитьЧтоСерверыРазбираютАдресОдинаково() Экспорт

	// Дано
	Адреса = АдресаДляСверкиРазбора();

	// Когда
	ВключитьСервер(Истина);
	РазборПрикладного = РазборАдресов(Адреса);

	ВключитьСервер(Ложь);
	РазборНативного = РазборАдресов(Адреса);

	// Тогда
	Ожидаем.Что(РазборНативного, "разбор адресов нативным сервером").Равно(РазборПрикладного);

КонецПроцедуры

Функция АдресаДляСверкиРазбора()

	Адреса = Новый Массив;
	Адреса.Добавить("echourl/a%2Fb");         // закодированная косая черта
	Адреса.Добавить("echourl/a%2fb");         // и она же в нижнем регистре
	Адреса.Добавить("echourl/dbl%2520enc");   // декодирование ровно один раз
	Адреса.Добавить("echourl/a%20b");
	Адреса.Добавить("echourl/a+b");           // плюс в пути - обычный символ
	Адреса.Добавить("echourl/a.b~c");
	Адреса.Добавить("echourl/%D0%B0%D0%B1");  // кириллица
	Адреса.Добавить("echourl/%d0%b0%d0%b1");  // и она же в нижнем регистре
	Адреса.Добавить("echourl/раз?x=a%2Bb");   // строка параметров не декодируется
	Адреса.Добавить("echourl/раз?");          // пустая строка параметров
	Адреса.Добавить("echourl/раз?x=1&x=2");   // повторяющийся ключ

	Возврат Адреса;

КонецФункции

Функция РазборАдресов(Адреса)

	Разбор = Новый Массив;

	Для Каждого Адрес Из Адреса Цикл
		Ответ = ГетЗапрос(Адрес);
		Ожидаем.Что(Ответ.КодСостояния, "код состояния для " + Адрес).Равно(200);
		Разбор.Добавить(Адрес + " => " + Ответ.ПолучитьТелоКакСтроку());
	КонецЦикла;

	Возврат СтрСоединить(Разбор, Символы.ПС);

КонецФункции

// Закодированная косая черта - символ внутри сегмента, а не разделитель сегментов.
// Раскодируй её заранее - и маршрутизация увидит лишний сегмент, а отдача файлов
// получит протащенный разделитель пути.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьЧтоЗакодированнаяКосаяЧертаНеРазделительСегментов(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Когда
	Ответ = ГетЗапрос("echourl/a%2Fb");

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Части = СтрРазделить(Ответ.ПолучитьТелоКакСтроку(), "|");
	Ожидаем.Что(Части[0], "путь запроса").Равно("/tests/echourl/a%2Fb");
	Ожидаем.Что(Части[2], "сегмент маршрута").Равно("a%2Fb");

КонецПроцедуры

// Путь декодируется ровно один раз, иначе %2520 схлопывается в пробел вместо %20.
// Прикладной сервер разбирает сырой HTTP-текст сам, нативному путь отдаёт Kestrel
// уже декодированным - обе реализации должны прийти к одному результату.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьЧтоПутьДекодируетсяОдинРаз(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Когда
	Ответ = ГетЗапрос("echourl/dbl%2520enc");

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Части = СтрРазделить(Ответ.ПолучитьТелоКакСтроку(), "|");
	Ожидаем.Что(Части[0], "путь запроса").Равно("/tests/echourl/dbl%20enc");

КонецПроцедуры

// Строка параметров в полном пути остаётся такой, какой пришла: по HTTP semantic
// conventions url.query отдаётся в исходном виде, иначе %2B становится неотличим
// от литерального плюса.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьЧтоСтрокаПараметровНеДекодируется(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Когда
	Ответ = ГетЗапрос("echourl/раз?x=a%2Bb");

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Части = СтрРазделить(Ответ.ПолучитьТелоКакСтроку(), "|");
	Ожидаем.Что(Части[1], "полный путь").Равно("/tests/echourl/раз?x=a%2Bb");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуУпорядоченныхГетПараметров(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "getorderedparams/Кошка/Собака";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Кошка И Собака");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуПостЗапроса(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "postparams";
	ТелоЗапроса = "animal1=Кошка&animal2=Собака";

	// Когда
	Ответ = ПостЗапросТелоИзСтроки(АдресТеста, ТелоЗапроса);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Кошка И Собака");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьРаботуКук(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	ЗначениеКуки = Строка(Новый УникальныйИдентификатор());
	ДатаИстечения = МестноеВремя('19700101235959');
	АдресТеста = СтрШаблон("setcookie?pechenka=%1&expires=%2", ЗначениеКуки, XMLСтрока(ДатаИстечения));

	// Когда
	Ответ = ГетЗапрос(АдресТеста);
	ТекстКук = Ответ.Заголовки.Получить("Set-Cookie");


	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(ТекстКук).Содержит(ЗначениеКуки);
	Ожидаем.Что(ТекстКук).Содержит("Thu, 01 Jan 1970 23:59:59 GMT");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьРаботуСессий(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	Значение = Строка(Новый УникальныйИдентификатор());
	АдресУстановки = СтрШаблон("setsessiondata?userdata=%1", Значение);
	АдресПолучения = "readsessiondata";

	// Когда
	ОтветУстановки = ГетЗапрос(АдресУстановки);
	ТекстКук = ОтветУстановки.Заголовки.Получить("Set-Cookie");
	МеткаСессии = "SessionID=";
	НачалоИдСессии = СтрНайти(ТекстКук,МеткаСессии);
	КонецИдСесии = СтрНайти(ТекстКук,";",,НачалоИдСессии);
	ИдСессии = СтрЗаменить(Сред(ТекстКук, НачалоИдСессии, КонецИдСесии - НачалоИдСессии), МеткаСессии, "");

	Заголовки = Новый Соответствие();
	Заголовки.Вставить("Cookie", "SessionID=" + ИдСессии);
	ОтветПолучения = ГетЗапрос(АдресПолучения, Заголовки);

	// Тогда
	Ожидаем.Что(ОтветУстановки.КодСостояния).Равно(200);
	Ожидаем.Что(ОтветПолучения.КодСостояния).Равно(200);
	Ожидаем.Что(ОтветПолучения.ПолучитьТелоКакСтроку()).Равно(Значение);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьПолучениеФайлаССервера(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	ЗначениеКуки = Строка(Новый УникальныйИдентификатор());
	АдресТеста = "textfile.txt";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);
	ДДТело = Ответ.ПолучитьТелоКакДвоичныеДанные();
	ТекстТело = ПолучитьСтрокуИзДвоичныхДанных(ДДТело);

	Чтение = Новый ЧтениеТекста();
	Чтение.Открыть("./tests/app/files/textfile.txt", КодировкаТекста.UTF8);
	ТекстИзФайла = Чтение.Прочитать();
	Чтение.Закрыть();


	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(ТекстТело).Равно(ТекстИзФайла);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОграничениеДоступа(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "user";
	Логипас = "Пользователь:111";
	Заголовки = Новый Соответствие();
	ХешЛогипасов = Base64Строка(ПолучитьДвоичныеДанныеИзСтроки(Логипас));
	Заголовки.Вставить("Authorization", "Basic " + ХешЛогипасов);

	ЗаголовкиНеКорректные = Новый Соответствие();
	ХешЛогипасовНеКорректные = Base64Строка(ПолучитьДвоичныеДанныеИзСтроки(Логипас+"1"));
	ЗаголовкиНеКорректные.Вставить("Authorization", "Basic " + ХешЛогипасовНеКорректные);


	// Когда
	ОтветЗапросАвторизации = ГетЗапрос(АдресТеста);
	ОтветАвторизован = ГетЗапрос(АдресТеста, Заголовки);
	ОтветНеАвторизован = ГетЗапрос(АдресТеста, ЗаголовкиНеКорректные);

	// Тогда
	Ожидаем.Что(ОтветЗапросАвторизации.КодСостояния).Равно(401);
	Ожидаем.Что(ОтветАвторизован.КодСостояния).Равно(200);
	Ожидаем.Что(ОтветНеАвторизован.КодСостояния).Равно(401);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьРаботуШаблонов(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "view/1";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку(КодировкаТекста.UTF8)).Равно("Заголовок 1 Текст 2 Вложенный текст 3 4 конец");
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	
КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьРаботуШаблоновБезГлобальногоОтображения(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "view/2";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку(КодировкаТекста.UTF8)).Равно("ТекстБезЗаголовка 1");
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	
КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьШаблонныеУРЛЫ(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "templateuri/Малинка/Желудь/Картошка";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Малинка Желудь Картошка");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьШаблонныеУРЛЫСложение(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "calc/plus/1/2";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("3");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьШаблонныеУРЛЫВычитание(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "calc/minus/3/2";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("1");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьШаблонныеУРЛУмножение(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "calc/3/multiply/2";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("6");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьШаблонныеУРЛЫДеление(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "calc/6/devide/2";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("3");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуГетПараметровПоИмени(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "getparamsbyname?ИмяКошки=Мурка&ИмяСобаки=Жучка";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("ИмяКошки=Мурка И ИмяСобаки=Жучка");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуПостЗапросаКакОбъект(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "postjsonbody";
	ТелоЗапроса = "{""Имя"": ""Иван"", ""Фамилия"": ""Пупкин""}";
	Заголовки = Новый Соответствие();
	Заголовки.Вставить("Content-Type", "application/json");

	// Когда
	Ответ = ПостЗапросТелоИзСтроки(АдресТеста, ТелоЗапроса, Заголовки);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Иван Пупкин");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуПостЗапросаКакТипизированныйОбъект(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "postjsontypedbody";
	ТелоЗапроса = "{""name"": ""Иван"", ""surname"": ""Пупкин""}";
	Заголовки = Новый Соответствие();
	Заголовки.Вставить("Content-Type", "application/json");

	// Когда
	Ответ = ПостЗапросТелоИзСтроки(АдресТеста, ТелоЗапроса, Заголовки);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("Иван Пупкин");

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьОбработкуПостЗапросаКакФорма(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "postformbody";
	ТелоЗапроса = "Имя=ivan&Фамилия=pupkin";
	Заголовки = Новый Соответствие();
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");

	// Когда
	Ответ = ПостЗапросТелоИзСтроки(АдресТеста, ТелоЗапроса, Заголовки);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("ivan pupkin");

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура Должен_ПроверитьРукопожатиеВебСокета(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	Клиент = Новый TCPСоединение("localhost", Порт);

	ТекстЗапроса = СтрШаблон("GET / HTTP/1.1
					|Host: localhost:%1
					|User-Agent: Mozilla/5.0 (Macintosh; Intel Mac OS X 10.15; rv:107.0) Gecko/20100101 Firefox/107.0
					|Accept: */*
					|Accept-Language: ru-RU,ru;q=0.8,en-US;q=0.5,en;q=0.3
					|Accept-Encoding: gzip, deflate, br
					|Sec-WebSocket-Version: 13
					|Origin: null
					|Sec-WebSocket-Extensions: permessage-deflate
					|Sec-WebSocket-Key: 8Nm7+HLTwID0n9ABpH5QGA==
					|DNT: 1
					|Connection: keep-alive, Upgrade
					|Sec-Fetch-Dest: websocket
					|Sec-Fetch-Mode: websocket
					|Sec-Fetch-Site: cross-site
					|Pragma: no-cache
					|Cache-Control: no-cache
					|Upgrade: websocket", Порт);

	// Когда
	Клиент.ОтправитьСтроку(ТекстЗапроса);
	Ответ = Клиент.ПрочитатьСтроку();
	Приостановить(100);

	// Тогда

	Ожидаем.Что(Клиент.Активно, "Соединение активно").Равно(Истина);
	Ожидаем.Что(Ответ, "ответ содержит хендшейк").Содержит("Sec-WebSocket-Accept: hgGtYQoki4w7EXHvdkxAVu61+PI=");
	Ожидаем.Что(Ответ, "ответ содержит код 101").Содержит("HTTP/1.1 101");
	
КонецПроцедуры

// &Тест
Процедура Должен_ПроверитьВебСокета() Экспорт

	// Дано
	Клиент = Новый TCPСоединение("localhost", Порт);
	// Клиент.ТаймаутЧтения = 1000;

	Запрос1 = Новый ДвоичныеДанные("tests/features/server1");
	Запрос2 = Новый ДвоичныеДанные("tests/features/server2");

	// Результат1 = Новый ДвоичныеДанные("tests/features/client1");
	Результат2 = Новый ДвоичныеДанные("tests/features/client2");

	// Когда
	Клиент.ОтправитьДвоичныеДанные(Запрос1);
	Ответ1 = Клиент.ПрочитатьСтроку();

	Приостановить(100);

	Клиент.ОтправитьДвоичныеДанные(Запрос2);
	Ответ2 = Клиент.ПрочитатьДвоичныеДанные();

	// Тогда

	Ожидаем.Что(Клиент.Активно, "Соединение активно").Равно(Истина);
	Ожидаем.Что(Ответ1, "ответ содержит хендшейк").Содержит("Sec-WebSocket-Accept: LEHPu4/qNukG+0hW+UjpnJTeoc8=");
	Ожидаем.Что(Ответ1, "ответ содержит код 101").Содержит("HTTP/1.1 101");
	Ожидаем.Что(Ответ2, "Ответ веб сокета корректный").Равно(Результат2);
	
КонецПроцедуры


Процедура Должен_ПроверитьРаспаковкуСообщенийВебСокета() Экспорт
	// Дано
	Шифровальщик = Новый ШифрованиеВебСокета();

	Каталог = ТекущийСценарий().Каталог;
	
	Запрос2 = Новый ДвоичныеДанные(ОбъединитьПути(Каталог, "features/server2"));

	// Когда
	Сообщение = Шифровальщик.РаспаковатьСообщение(Запрос2);

	// Тогда
	Ожидаем.Что(Сообщение, "Распаковка удачно").Равно("hello");

КонецПроцедуры


Процедура Должен_ПроверитьУпаковкуСообщенийВебСокета() Экспорт
	// Дано
	Шифровальщик = Новый ШифрованиеВебСокета();

	Каталог = ТекущийСценарий().Каталог;
	
	УпакованноеСообщение = Новый ДвоичныеДанные(ОбъединитьПути(Каталог, "features/client2"));

	// Когда
	Сообщение = Шифровальщик.ЗапаковатьСообщениеТекстовоеСообщение("hello 1");

	// Тогда
	Ожидаем.Что(Сообщение, "Упаковка удачно").Равно(УпакованноеСообщение);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПолучениеСоединенийСотборами(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	СоединенияВебСокетов = Новый СоединенияВебСокетов();
	Рефлектор = Новый Рефлектор();
	ПулСоединений = Рефлектор.ПолучитьСвойство(СоединенияВебСокетов, "ПулСоединений");

	НовоеСоединение = ПулСоединений.Добавить();
	НовоеСоединение.Идентификатор = "1";
	НовоеСоединение.Топик = "1";

	НовоеСоединение = ПулСоединений.Добавить();
	НовоеСоединение.Идентификатор = "2";
	НовоеСоединение.Топик = "1";

	НовоеСоединение = ПулСоединений.Добавить();
	НовоеСоединение.Идентификатор = "3";
	НовоеСоединение.Топик = "1";

	НовоеСоединение = ПулСоединений.Добавить();
	НовоеСоединение.Идентификатор = "1";
	НовоеСоединение.Топик = "2";

	СписокИдентификаторов = Новый Массив();
	СписокИдентификаторов.Добавить("1");
	СписокИдентификаторов.Добавить("2");

	// Когда

	ВсеСоединения = СоединенияВебСокетов.ПолучитьСоединенияПоТопику("1");

	СоединенияПоСписку = СоединенияВебСокетов.ПолучитьСоединенияПоТопикуСпискуИдентификаторов("1", СписокИдентификаторов);

	СоединенияКроме = СоединенияВебСокетов.ПолучитьСоединенияПоТопикуКромеСпискаИдентификаторов("1", СписокИдентификаторов);

	СоединениеПоИдентификатору = СоединенияВебСокетов.ПолучитьСоединениеПоИдентификатору("1", "1");

	СоединениеНеопределено = СоединенияВебСокетов.ПолучитьСоединениеПоИдентификатору("1", "9");

	// Тогда
	Ожидаем.Что(ВсеСоединения.Количество(), "Все соединения").Равно(3);
	Ожидаем.Что(СоединенияПоСписку.Количество(), "Соединения по списку").Равно(2);
	Ожидаем.Что(СоединенияКроме.Количество(), "Соединения кроме").Равно(1);
	Ожидаем.Что(СоединениеПоИдентификатору.Идентификатор, "Конкретное соединение").Равно("1");
	Ожидаем.Что(СоединениеНеопределено, "пустое соединение").Равно(Неопределено);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьШаблонныеУРЛЫИзКорневогоКонтрола(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано

	Соединение = Новый HTTPСоединение("http://localhost",Порт,,,,5);
	Запрос = Новый HTTPЗапрос("/api/некоторыйпараметр");

	Ответ = Соединение.Получить(Запрос);

	// Когда
	Ответ = Соединение.Получить(Запрос);

	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(Ответ.ПолучитьТелоКакСтроку()).Равно("некоторыйпараметр");

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура ССЕКлиентПодключился(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	КонтролССЕ = Поделка.НайтиЖелудь("КонтролССЕ");
	КонтролССЕ.ИнициализироватьСостояния();
	ССЕКлиент = Новый ССЕКлиент("localhost", Порт, "/sse/test1");

	// Кода

	// Тогда

	Ожидаем.Что(ССЕКлиент.ПервичныйОтвет()).Содержит("200");

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура ССЕОтправитьВсем(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	КонтролССЕ = Поделка.НайтиЖелудь("КонтролССЕ");
	КонтролССЕ.ИнициализироватьСостояния();
	ССЕКлиент1 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");
	ССЕКлиент2 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");

	ТекстСообщения = "Привет";

	// Когда
	ПроизвольныйГетЗапрос("/sse/sendall/" + ТекстСообщения);

	Приостановить(1000);

	Сообщения1 = ССЕКлиент1.Сообщения();
	Сообщения2 = ССЕКлиент2.Сообщения();

	// Тогда
	Ожидаем.Что(Сообщения1.Количество(), "Сообщения1").Равно(1);
	Ожидаем.Что(Сообщения2.Количество(), "Сообщения2").Равно(1);

	Ожидаем.Что(Сообщения1[0], "Сообщение1").Равно("data: " + ТекстСообщения);
	Ожидаем.Что(Сообщения2[0], "Сообщение2").Равно("data: " + ТекстСообщения);

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура ССЕОтправитьВсемКастомныйТип(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	КонтролССЕ = Поделка.НайтиЖелудь("КонтролССЕ");
	КонтролССЕ.ИнициализироватьСостояния();
	ССЕКлиент1 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");

	ТекстСообщения = "Привет";

	// Когда
	ПроизвольныйГетЗапрос("/sse/sendalltype/" + ТекстСообщения);

	Приостановить(1000);

	Сообщения1 = ССЕКлиент1.Сообщения();

	// Тогда
	Ожидаем.Что(Сообщения1.Количество(), "Сообщения1").Равно(1);

	Ожидаем.Что(Сообщения1[0], "Сообщение1").Равно("event: evnt" + Символы.ПС + "data: " + ТекстСообщения);

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура ССЕОтправитьТолько(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	КонтролССЕ = Поделка.НайтиЖелудь("КонтролССЕ");
	КонтролССЕ.ИнициализироватьСостояния();
	ССЕКлиент1 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");
	ССЕКлиент2 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");

	// Когда
	ПроизвольныйГетЗапрос("/sse/sendbyid/" + КонтролССЕ.ИдПодключений[0]);

	Приостановить(1000);

	Сообщения1 = ССЕКлиент1.Сообщения();
	Сообщения2 = ССЕКлиент2.Сообщения();

	// Тогда
	Ожидаем.Что(КонтролССЕ.ИдПодключений.Количество(), "Количество соединений").Равно(2);
	Ожидаем.Что(Сообщения1.Количество(), "Сообщения1").Равно(1);
	Ожидаем.Что(Сообщения2.Количество(), "Сообщения2").Равно(0);

	Ожидаем.Что(Сообщения1[0], "Сообщение1").Равно("data: привет");

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура ССЕОтправитьКроме(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	КонтролССЕ = Поделка.НайтиЖелудь("КонтролССЕ");
	КонтролССЕ.ИнициализироватьСостояния();
	ССЕКлиент1 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");
	ССЕКлиент2 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");
	ССЕКлиент3 = Новый ССЕКлиент("localhost", Порт, "/sse/test1");

	// Когда
	ПроизвольныйГетЗапрос("/sse/sendbut/" + КонтролССЕ.ИдПодключений[0]);

	Приостановить(1000);

	Сообщения1 = ССЕКлиент1.Сообщения();
	Сообщения2 = ССЕКлиент2.Сообщения();
	Сообщения3 = ССЕКлиент3.Сообщения();

	// Тогда
	Ожидаем.Что(КонтролССЕ.ИдПодключений.Количество(), "Количество соединений").Равно(3);
	Ожидаем.Что(Сообщения1.Количество(), "Сообщения1").Равно(0);
	Ожидаем.Что(Сообщения2.Количество(), "Сообщения2").Равно(1);
	Ожидаем.Что(Сообщения3.Количество(), "Сообщения3").Равно(1);

	Ожидаем.Что(Сообщения2[0], "Сообщение2").Равно("data: привет");
	Ожидаем.Что(Сообщения3[0], "Сообщение3").Равно("data: привет");

КонецПроцедуры

// Нативный сервер не поддерживает WebSocket и Server-Sent Events,
// поэтому тест выполняется только для прикладной реализации.
&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
Процедура ССЕПодпискаНаЗакрытие(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	КонтролССЕ = Поделка.НайтиЖелудь("КонтролССЕ");
	КонтролССЕ.ИнициализироватьСостояния();
	ССЕКлиент1 = Новый ССЕКлиент("localhost", Порт, "/sse/testsub");
	ССЕКлиент2 = Новый ССЕКлиент("localhost", Порт, "/sse/testsub");
	ССЕКлиент3 = Новый ССЕКлиент("localhost", Порт, "/sse/testsub");

	// Когда

	ССЕКлиент1.Остановить();
	
	Приостановить(20000);

	// Тогда
	Ожидаем.Что(КонтролССЕ.КоличествоЗакрытыхСоединений, "Количество закрытых соединений").Равно(1);

КонецПроцедуры

&ПараметризованныйТест(Имя = "прикладной={Параметры}")
&ИсточникЗначение(Истина)
&ИсточникЗначение(Ложь)
Процедура Должен_ПроверитьПолучениеФайлаКастомногоУрла(Прикладной) Экспорт

	ВключитьСервер(Прикладной);

	// Дано
	АдресТеста = "customefilename";

	// Когда
	Ответ = ГетЗапрос(АдресТеста);
	ДДТело = Ответ.ПолучитьТелоКакДвоичныеДанные();
	ТекстТело = ПолучитьСтрокуИзДвоичныхДанных(ДДТело);

	Чтение = Новый ЧтениеТекста();
	Чтение.Открыть("./tests/app/files/textfile.txt", КодировкаТекста.UTF8);
	ТекстИзФайла = Чтение.Прочитать();
	Чтение.Закрыть();


	// Тогда
	Ожидаем.Что(Ответ.КодСостояния).Равно(200);
	Ожидаем.Что(ТекстТело).Равно(ТекстИзФайла);

КонецПроцедуры
