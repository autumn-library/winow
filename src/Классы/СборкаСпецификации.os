// BSLLS:MissingVariablesDescription-off

// Состояние одной сборки документа OpenAPI.
//
// Спецификация требует уникальности operationId и ключей components в пределах
// одного документа - значит, ровно столько и должны жить реестры выданных имён
// и собранные схемы.
//
// Держать их на желудях нельзя: желудь один на приложение, а документов
// одновременно собирается столько, сколько пришло запросов за спецификацией.
// Реестры разъезжались бы, и операции получали бы числовые суффиксы от чужой
// сборки: get_catalog вместо себя становился get_catalog_3.
//
// Объект создаётся в начале сборки и передаётся по вызовам, поэтому желудям
// собственного состояния не нужно, а сборка получается повторно входимой.

// Соответствие Из Строка - выданные идентификаторы операций и число их выдач.
Перем ВыданныеИдентификаторы Экспорт;

// Соответствие Из Строка - выданные имена схем и число их выдач.
Перем ВыданныеИменаСхем Экспорт;

// Соответствие Из Тип - имя схемы, уже выданное типу. Одному типу - одно имя
// на весь документ: иначе ссылка $ref разъехалась бы со схемой.
Перем ИменаТипов Экспорт;

// Соответствие Из Соответствие - собранные схемы по их именам. Отдаётся
// наружу как объект components/schemas документа.
Перем Схемы Экспорт;

// Соответствие Из Строка - описания тегов, собранные при обходе операций.
Перем ОписанияТегов Экспорт;

Процедура ПриСозданииОбъекта()

	ВыданныеИдентификаторы = Новый Соответствие();
	ВыданныеИменаСхем = Новый Соответствие();
	ИменаТипов = Новый Соответствие();
	Схемы = Новый Соответствие();
	ОписанияТегов = Новый Соответствие();

КонецПроцедуры
