#Использовать autumn

Функция СоздатьСервер() Экспорт
	КонтекстПриложения = Новый КонтекстПриложения();

	КаталогМодуля = ТекущийСценарий().Каталог;
	КаталогКлассов = ОбъединитьПути(КаталогМодуля, "../Классы");

	КонтекстПриложения.ПросканироватьКаталог(КаталогКлассов);

	Возврат КонтекстПриложения.ПолучитьЖелудь("ВебСервер");

КонецФункции
